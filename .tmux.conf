# Mouse OFF - VS Code handles copy/paste/right-click
set -g mouse off

# Set prefix to Ctrl+b (default)
set -g prefix C-b
unbind C-a
bind C-b send-prefix

# Scroll: Ctrl+b [ then PgUp/Dn or arrows, q to exit
